# Current Design

Store current architecture, module boundaries, public contracts, data models, states, workflows, failure boundaries, side effects, and verification entry points here.

Use `NN-topic.md` names when ordered navigation is useful. State whether described behavior is implemented, planned, deprecated, or awaiting validation.
