name: dsh_tablet_client
description: DSH Tablet Client - RK3288平板 AI Agent 前端
publish_to: 'none'
version: 1.3.1+10

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^2.4.0
  shared_preferences: ^2.2.2
  uuid: ^4.2.1
  intl: ^0.19.0
  provider: ^6.1.1
  wakelock_plus: ^1.2.5
  flutter_foreground_task: ^8.9.1
  flutter_markdown: ^0.7.7+1
  audioplayers: ^6.8.1
  file_picker: ^12.2.0
  path_provider: ^2.1.6
  package_info_plus: ^10.2.1
  mobile_scanner: ^6.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
